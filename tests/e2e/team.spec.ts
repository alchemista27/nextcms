import { test, expect } from '@playwright/test';

test.describe('Team Admin', () => {
  test('Team list page exists', async ({ page }) => {
    // Just a placeholder until auth is mocked for E2E
    expect(true).toBe(true);
  });
});
